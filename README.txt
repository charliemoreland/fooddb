#This was an assignment for a relational database management class
#execute some queries 
#Create a stored procedure to return food with amount of ingredients of category meat that you want and more grams total that you want
#Create a trigger if a recipe is inserted with a 0 or negative amount to display an error message
